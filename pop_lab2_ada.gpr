project Pop_Lab2_Ada is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("test.adb", "main.adb");

   package Builder is
      for Switches ("ada") use ("-g");
   end Builder;

   package Compiler is
      for Switches ("ada") use ("-g");
   end Compiler;

   package Linker is
      for Switches ("ada") use ("-g");
   end Linker;

end Pop_Lab2_Ada;

